# Pinned to the interpreter and libraries embedded in Front Arena.
# The FA CPython is 3.9.7 and cannot be upgraded, so everything here must
# import and run on 3.9 (no PEP 604 `X | Y` unions at runtime, no `match`, etc.).
#
#   python 3.9.7
numpy==1.22.4
scipy==1.13.1
